<% content_for(:title, @tab.page_title) %>
<div class="ndl-details">
  <h3 class="ndl-debug">Debug ID: <%= @tab.record_id %></h3>

  <%= render(partial: 'detail_section', locals: { title_key: false, content: @tab.detail_content(@tab.detail_methods) } ) %>

  <%= render(partial: 'detail_section', locals: { title_key: 'other_titles', content: @tab.detail_content(@tab.other_titles_methods) } ) %>

  <%= render(partial: 'detail_section', locals: { title_key: 'related_works', content: @tab.detail_content(@tab.related_works_methods) } ) %>

  <%= render(partial: 'detail_section', locals: { title_key: 'identifiers', content: @tab.detail_content(@tab.identifiers_methods) } ) %>
</div>
